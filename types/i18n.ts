// Languages the app supports (currently just English)
export type SupportedLocale = "en";

// Currencies the app can display prices in (currently just PKR)
export type SupportedCurrency = "PKR";

// A flat map of translation keys to their display text
export type TranslationDictionary = Record<string, string>;